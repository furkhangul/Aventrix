// Root build file. Version numbers below were current as of this project's
// authoring but are NOT verified against Maven Central from this environment
// (no network/JDK access here) — Android Studio will flag any that have since
// moved; accept its update suggestions rather than treating these as pinned.
plugins {
    id("com.android.application") version "8.7.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
    id("org.jetbrains.kotlin.plugin.serialization") version "2.0.21" apply false
}
